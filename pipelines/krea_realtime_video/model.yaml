model_name: Wan2.1-T2V-1.3B
model_kwargs:
  timestep_shift: 5.0
denoising_step_list:
- 1000
- 750
- 500
- 250
warp_denoising_step: true
num_frame_per_block: 3

# VAE configuration (optional)
# By default, uses vendor-optimized VAE
# Uncomment to use factory VAE (supports auto-download):
vae: lightvae2.1  # 75% pruned, lower VRAM
