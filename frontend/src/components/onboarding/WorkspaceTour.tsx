/**
 * WorkspaceTour is currently disabled. The component and its dependencies
 * (TourPopover, tourSteps) are preserved for future re-enablement.
 */
export function WorkspaceTour() {
  return null;
}
